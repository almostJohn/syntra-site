import * as React from "react";

export function Note() {
	return (
		<header className="sticky top-0 z-50 w-full bg-red-600 text-white">
			<div className="container flex items-center justify-center px-2 py-1 text-xs text-center font-medium">
				Starting February 3, 2025 (02/03/2025) simplynote is now a standalone
				notepad website!
			</div>
		</header>
	);
}
